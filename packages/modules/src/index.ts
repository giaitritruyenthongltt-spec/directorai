/**
 * @directorai/modules — Khung module edit (signals → judge → execute).
 *
 * Registry canonical cho danh sách "năng lực edit". AutoTab + server đọc
 * chung từ đây; thêm chức năng = +1 object trong registry.ts.
 */

export * from './types.js';
export * from './registry.js';
export * from './pipeline.js';
