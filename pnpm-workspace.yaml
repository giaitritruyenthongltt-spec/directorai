packages:
  - "apps/*"
  - "packages/*"
  - "plugins/*"
  # P1a — "future/*" parked (orphan scaffold không thuộc MVP): KHÔNG build/cài.
  - "!future/**"

allowBuilds:
  "@sentry/cli": set this to true or false
  esbuild: true
  sharp: set this to true or false
