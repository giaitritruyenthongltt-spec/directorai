/**
 * Thin re-export — the canonical dispatcher now lives in
 * @directorai/premiere-adapter so both server and panel can share it.
 */
export { dispatchRpc, listRpcMethods } from '@directorai/premiere-adapter';
