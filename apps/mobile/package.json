{
  "name": "@directorai/mobile",
  "version": "0.1.0",
  "private": true,
  "description": "DirectorAI mobile companion (iOS + Android, Expo bare workflow).",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit"
  },
  "_owner_completed": {
    "ios": "Apple Developer account + Xcode + RN environment (Track C C.13)",
    "android": "Play Console + Android Studio + signing keys (Track C C.13)",
    "expo": "npx create-expo-app --template default once certs land"
  },
  "dependencies": {
    "@directorai/sdk": "workspace:*",
    "@directorai/style-engine": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "vitest": "^2.1.2"
  }
}
